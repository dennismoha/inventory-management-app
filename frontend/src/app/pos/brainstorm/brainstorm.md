# goal, to design and code the interface for the pos

So where do we start ?
what do we want to render ?
    1) products
    2) suppliers
    3) customers
    5) inventory

page requirements:
    font color,
    line height,
    layout, 
    image size,
    grids/ flexbox ?
    etc.